# Simple Alarm Clock

A minimal browser-based alarm clock — no dependencies, single HTML file.

## Features

- Set multiple alarms with optional labels
- Enable / disable each alarm individually
- Audible beep via Web Audio API
- Browser notifications
- Alarms persist across page refreshes (localStorage)

## Usage

Open `alarm.html` in any modern browser. No server needed.
